Unit 0 Unix
gunzip...
cut,grep, []

Unit 1 Calculation
integer divide //	remainder %

abs(x) |x|	pow(x,y)  x^y	round(x, ndigits=3)

math.ceil(x) up	math.floor(x) down	sqrt=square root
math.factorial(n) n! inf=infinity
